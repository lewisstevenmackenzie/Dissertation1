rules:
  - rule: activate module choice form
    steps:
      - intent: chooseModule
      - action: module_form
      - active_loop: module_form

  - rule: submit module form
    condition:
      - active_loop: module_form
    steps:
      - action: module_form
      - active_loop: null
      - slot_was_set:
          - requested_slot: null
      - action: utter_submit
      - action: action_repeat_values
      - action: action_recommend_similar_keywords

  - rule: Form interrupted with field Query
    condition:
      # Condition that form is active.
      - active_loop: module_form
    steps:
      - intent: fieldQuery
      #- action: utter_field_list
      - action: action_list_study_fields
        # Return to form after handling the `chitchat` intent
      - action: module_form
      - active_loop: module_form

  - rule: Form interrupted with field unsure
    condition:
      # Condition that form is active.
      - active_loop: module_form
    steps:
      - intent: unsureOnStudyField
      #- action: utter_field_list
      - action: action_list_study_fields
        # Return to form after handling the `chitchat` intent
      - action: module_form
      - active_loop: module_form

  - rule: out-of-scope
    steps:
      - intent: out_of_scope
      - action: utter_out_of_scope

  - rule: Ask the user to rephrase whenever they send a message with low NLU confidence
    steps:
      - intent: nlu_fallback
      - action: utter_please_rephrase

  - rule: user asks for list of modules
    steps:
      - intent: listModules
      - action: action_list_modules

  - rule: user is unsure which study field they want to go into.
    steps:
      - intent: unsureOnStudyField
      - action: action_list_study_fields

  - rule: user asks about Visa's
    steps:
      - intent: visaQuestions
      - action: action_send_staff_email

  # - rule: testing recommendation
  #   steps:
  #     - intent: recommend
  #     - action: action_recommend_similar_description
  # - rule: State what the module involves
  #   steps:
  #     - intent: moduleQuery
  #       entities:
  #         - module: Computer Systems
  #     - slot_was_set:
  #         - module: Computer Systems
  #     - action: utter_module_specifications
  #     - action: action_module_specifications
